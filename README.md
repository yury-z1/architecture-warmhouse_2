# Project_template

Это шаблон для решения проектной работы. Структура этого файла повторяет структуру заданий. Заполняйте его по мере работы над решением.

---

## Задание 1. Анализ и планирование

Чтобы составить документ с описанием текущей архитектуры приложения, можно часть информации взять из описания компании и условия задания. Это нормально.

### 1. Описание функциональности монолитного приложения

**Управление отоплением:**

Пользователи могут **включать/выключать отопление**, **задавать целевую температуру** по помещениям, **просматривать текущее состояние** устройств.  
Система поддерживает **аутентификацию пользователей**, **хранение параметров домов и устройств**, **синхронные HTTP-вызовы** к устройствам через монолит, **уведомления по email/SMS**.

**Мониторинг температуры:**

Пользователи могут **просматривать текущую температуру** и **историю измерений**.  
Система поддерживает **опрос датчиков в синхронном режиме**, **сохранение показаний в БД**, **агрегацию/выборку за период** через монолит.

### 2. Анализ архитектуры монолитного приложения

Перечислите здесь основные особенности текущего приложения: какой язык программирования используется, какая база данных, как организовано взаимодействие между компонентами и так далее.

- **Язык:** Go  
- **База данных:** PostgreSQL (единая БД для всех функций)  
- **Коммуникации:** синхронные HTTP-запросы; внутренняя логика и доступ к данным — в одном процессе  
- **Развёртывание:** единый артефакт; релизы требуют перезапуска всего приложения  
- **Масштабирование:** вертикальное; нет независимого масштабирования подсистем  
- **Наблюдаемость:** базовые логи; метрики/трейсы ограничены

### 3. Определение доменов и границы контекстов

Опишите здесь домены, которые вы выделили.

- **Identity & Access (AuthN/AuthZ)** — пользователи, роли, политики доступа  
- **Device Registry / Provisioning** — учёт устройств/типов, подключение, статусы  
- **Telemetry (Ingest / Query)** — приём, хранение и выборка телеметрии  
- **Actuation** — Heating, Lighting, Gate, Surveillance (выдача команд)  
- **Rules / Orchestration** — сценарии/триггеры на события  
- **Billing / Plans** — тарифы, подписки

### 4. Проблемы монолитного решения

Если вы считаете, что текущее решение не вызывает проблем, аргументируйте свою позицию.

**Заполнено:**

- **Слабая масштабируемость:** невозможно независимо увеличивать мощности под ingest телеметрии или под API-запросы.  
- **Высокая связность:** изменения в одной области затрагивают весь код и повышают риск регрессий.  
- **Простои при релизах:** деплой монолита приводит к остановке всего приложения.  
- **Ограниченная наблюдаемость:** сложно локализовать проблемы по отдельным потокам, нет сквозной трассировки.  
- **Интеграции затруднены:** поддержка разных протоколов/партнёрских устройств усложняет монолит и замедляет выпуск фич.

### 5. Визуализация контекста системы — диаграмма С4

**Ссылка на диаграмму контекста:**  
[Диаграмма C4: Context](./docs/c4-context.puml)

---

## Задание 2. Проектирование микросервисной архитектуры

В этом задании вам нужно предоставить только диаграммы в модели C4. Мы не просим вас отдельно описывать получившиеся микросервисы и то, как вы определили взаимодействия между компонентами To-Be системы. Если вы правильно подготовите диаграммы C4, они и так это покажут.

**Диаграмма контейнеров (Containers)**

Добавьте диаграмму.  
**Ссылка:** [C4: Containers (To-Be)](./docs/c4-containers.puml)

**Диаграмма компонентов (Components)**

Добавьте диаграмму для каждого из выделенных микросервисов.  
**Ссылка (пример Device Registry & Telemetry):** [C4: Components](./docs/c4-components.puml)

**Диаграмма кода (Code)**

Добавьте одну диаграмму или несколько.  
**(при наличии последовательностей/классов — приложить ссылки здесь)**

---

## Задание 3. Разработка ER-диаграммы

Добавьте сюда ER-диаграмму. Она должна отражать ключевые сущности системы, их атрибуты и тип связей между ними.

**Ссылка:** [ER-диаграмма](./docs/er.puml)

---

## Задание 4. Создание и документирование API

### 1. Тип API

Укажите, какой тип API вы будете использовать для взаимодействия микросервисов. Объясните своё решение.

**Заполнено:**  
- **REST (OpenAPI 3.0)** — для управляющих и запросных операций (регистрация устройств, запрос телеметрии, команды отоплению); удобно версионировать и контролировать доступ (JWT/Scopes), простота и стандартизация, унификация взаимодействия между сервисами и SPA.  
- **События (AsyncAPI, pub/sub)** — для телеметрии и статусов устройств, а также для асинхронных команд/подтверждений; обеспечивает слабую связанность и горизонтальное масштабирование.

### 2. Документация API

Здесь приложите ссылки на документацию API для микросервисов, которые вы спроектировали в первой части проектной работы. Для документирования используйте Swagger/OpenAPI или AsyncAPI.

**Ссылки:**  
- **OpenAPI (REST):** [docs/api/openapi.yaml](./docs/api/openapi.yaml)  
- **AsyncAPI (Events):** [docs/api/asyncapi.yaml](./docs/api/asyncapi.yaml)

---

## Задание 5. Работа с docker и docker-compose

Перейдите в apps.

Там находится приложение-монолит для работы с датчиками температуры. В README.md описано как запустить решение.

Вам нужно:

сделать простое приложение temperature-api на любом удобном для вас языке программирования, которое при запросе /temperature?location= будет отдавать рандомное значение температуры.

Locations - название комнаты, sensorId - идентификатор названия комнаты

// If no location is provided, use a default based on sensor ID
if location == "" {
	switch sensorID {
	case "1":
		location = "Living Room"
	case "2":
		location = "Bedroom"
	case "3":
		location = "Kitchen"
	default:
		location = "Unknown"
	}
}

// If no sensor ID is provided, generate one based on location
if sensorID == "" {
	switch location {
	case "Living Room":
		sensorID = "1"
	case "Bedroom":
		sensorID = "2"
	case "Kitchen":
		sensorID = "3"
	default:
		sensorID = "0"
	}
}


Приложение следует упаковать в Docker и добавить в docker-compose. Порт по умолчанию должен быть 8081

Кроме того для smart_home приложения требуется база данных — добавьте в docker-compose файл настройки для запуска postgres с указанием скрипта инициализации ./smart_home/init.sql

Для проверки можно использовать Postman коллекцию smarthome-api.postman_collection.json и вызвать:

Create Sensor

Get All Sensors

Должно при каждом вызове отображаться разное значение температуры

Ревьюер будет проверять точно так же.

**Ссылки на реализованные артефакты этого задания:**  
- Учебный сервис: [`services/temperature-api/main.js`](./services/temperature-api/main.js)  
- Dockerfile: [`services/temperature-api/Dockerfile`](./services/temperature-api/Dockerfile)  
- Docker Compose: [`apps/docker-compose.yml`](./apps/docker-compose.yml)  
- Инициализация БД: [`apps/smart_home/init.sql`](./apps/smart_home/init.sql)  
- Postman-коллекция: [`apps/smarthome-api.postman_collection.json`](./apps/smarthome-api.postman_collection.json)


**Команды запуска/проверки:**

cd apps
docker compose up --build -d
# проверка
curl http://localhost:8081/temperature?location=living-room
